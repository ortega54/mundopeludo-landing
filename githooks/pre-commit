#!/bin/sh
# Antes de cada commit: sincroniza docs/ con la raiz y lo incluye en el commit.
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT" || exit 0
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$ROOT/scripts/sync-docs.ps1" || exit 1
git add docs/
